# Тёмная сине-зелёная тема
import customtkinter as ctk

COLORS = {
    "bg_primary": "#1a3a52",      # Тёмно-синий фон
    "bg_secondary": "#0d2137",    # Очень тёмный
    "accent": "#2d6a4f",          # Зелёный
    "accent_hover": "#1b4332",    # Тёмно-зелёный
    "text_primary": "#7ec8e3",    # Голубой текст
    "text_secondary": "#4a9eff",  # Яркий голубой
    "border": "#2d6a4f",          # Границы
    "success": "#2d6a4f",         # Успех
    "error": "#c75b5b",           # Ошибка
}
FONTS = {"title": ctk.CTkFont(family="Arial", size=24,
                              weight="bold"),
         "heading": ctk.CTkFont(family="Arial", size=16,
                                weight="bold"),
         "body": ctk.CTkFont(family="Arial", size=13),
         "small": ctk.CTkFont(family="Arial", size=11),
         "large": ctk.CTkFont(family="Arial", size=18, weight="bold")}
SPACING = {"xs": 4, "sm": 8, "md": 16, "lg": 24, "xl": 32}

def apply_theme():
    ctk.set_appearance_mode("dark")
    ctk.set_default_color_theme("dark-blue")
